# INDILEX — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core Data Processing ──
pandas>=2.0
numpy>=1.22
openpyxl>=3.1            # Excel file I/O
tqdm>=4.60               # Progress bars

# ── NLP & Language Detection ──
langdetect>=1.0.9        # Language detection for judgments

# ── LLM Inference (GPU required for extraction) ──
transformers>=4.42       # HuggingFace model loading (Qwen, Gemma, DeepSeek)
tokenizers>=0.20
accelerate>=0.26         # Multi-GPU / mixed precision
sentencepiece>=0.1.99    # Tokenizer backend
torch>=2.0               # PyTorch (install with CUDA support separately)
huggingface_hub>=0.24    # Model downloads

# ── Evaluation Metrics ──
rouge-score>=0.1.2       # ROUGE-1/2/L for reasoning evaluation
bert-score>=0.3.13       # BERTScore for semantic similarity
scikit-learn>=1.0        # Classification metrics

# ── Visualization ──
matplotlib>=3.5
seaborn>=0.12
wordcloud>=1.9           # Word cloud generation

# ── API-based Annotation ──
groq>=0.4                # Groq API for batch annotation
anthropic>=0.20          # Claude API for Hindi annotation fill
python-dotenv>=1.0       # Environment variable management

# ── Optional: Long-document annotation ──
# vllm>=0.4              # vLLM for Qwen2.5-14B (install separately)
